// Generated by CoffeeScript 1.10.0
var colors, initCommands;

colors = require('colors');

initCommands = function(program) {
  return program.command('ffmpeg').description('FFMPEG Utilities').option('-c, --convert <input>', 'Convert file to another format.').option('-f, --format <format>', 'The output format.').option('-o, --output <filename>', 'The output file.').action(function(options) {
    var FFMPEG;
    FFMPEG = require('./ffmpeg');
    if (options.convert) {
      return FFMPEG.convert({
        input: options.convert,
        format: options.format || null,
        output: options.output || null
      }).then(function(res) {
        return console.log(res);
      });
    }
  });
};

module.exports = initCommands;
